# Rust build artifacts
/target/
**/*.rs.bk
*.pdb

# Cargo lock (binary crates track it; library crates don't)
# We track Cargo.lock for the daemon binary
# Cargo.lock

# Kiro — ignore entirely (IDE workspace config, not project source)
.kiro/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs (runtime artifacts, not source)
*.log

# Environment
.env
.env.local

# Test artifacts
/test-output/
proptest-regressions/

# Build artifacts
/dist/
/build/

# Runtime files (created during execution, not source)
/models/
/checkpoints/
/audit.log

# PKI — private keys and org certs, never commit
coordinator/pki/
